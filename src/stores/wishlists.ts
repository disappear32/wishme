import { defineStore } from "pinia"

export const useWishlists = defineStore('wishlists', () => {
    const fetchLists = async () => {

    }

    

    return {
        fetchLists,
    }
})